generator client {
  provider        = "prisma-client-js"
  output          = "../src/generated/prisma"
  previewFeatures = ["driverAdapters"]
}

datasource db {
  provider = "postgresql"
}

model users {
  id                  Int                   @id @default(autoincrement())
  login               String                @unique @db.VarChar(50)
  first_name          String                @db.VarChar(50)
  last_name           String                @db.VarChar(50)
  email               String                @unique @db.VarChar(100)
  password_hash       String                @db.VarChar(255)
  phone_number        String?               @db.VarChar(15)
  role                String?               @default("driver") @db.VarChar(20)
  is_active           Boolean?              @default(true)
  is_employed         Boolean?              @default(true)
  created_at          DateTime?             @default(now()) @db.Timestamp(6)
  vehicle_assignments vehicle_assignments[]
}

model vehicles {
  id                    Int                     @id @default(autoincrement())
  license_plate         String                  @unique @db.VarChar(15)
  brand                 String                  @db.VarChar(50)
  model                 String                  @db.VarChar(50)
  year_of_manufacture   Int
  current_mileage       Int                     @default(0)
  current_fuel_level    Int                     @default(100)
  status                String?                 @default("available") @db.VarChar(20)
  created_at            DateTime?               @default(now()) @db.Timestamp(6)
  vehicle_assignments   vehicle_assignments[]
  vehicle_status_images vehicle_status_images[]
  vehicle_incidents     vehicle_incidents[]
}

model vehicle_assignments {
  id                  Int                 @id @default(autoincrement())
  vehicle_id          Int?
  driver_id           Int?
  start_time          DateTime            @default(now()) @db.Timestamp(6)
  end_time            DateTime?           @db.Timestamp(6)
  start_mileage       Int
  end_mileage         Int?
  start_fuel_level    Int
  end_fuel_level      Int?
  dashboard_image_url String?
  status              String?             @default("active") @db.VarChar(25)
  users               users?              @relation(fields: [driver_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  vehicles            vehicles?           @relation(fields: [vehicle_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  vehicle_incidents   vehicle_incidents[]
}

model vehicle_status_images {
  id             Int       @id @default(autoincrement())
  vehicle_id     Int?
  side           String    @db.VarChar(20)
  azure_blob_url String
  updated_at     DateTime? @default(now()) @db.Timestamp(6)
  vehicles       vehicles? @relation(fields: [vehicle_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([vehicle_id, side])
}

model vehicle_incidents {
  id                      Int                       @id @default(autoincrement())
  vehicle_id              Int
  assignment_id           Int?
  ai_description          String
  status                  String                    @default("pending")
  created_at              DateTime                  @default(now())
  vehicles                vehicles                  @relation(fields: [vehicle_id], references: [id], onDelete: Cascade)
  vehicle_assignments     vehicle_assignments?      @relation(fields: [assignment_id], references: [id], onDelete: SetNull)
  vehicle_incident_images vehicle_incident_images[]
}

model vehicle_incident_images {
  id                Int               @id @default(autoincrement())
  incident_id       Int
  side              String
  azure_blob_url    String
  image_type        String
  vehicle_incidents vehicle_incidents @relation(fields: [incident_id], references: [id], onDelete: Cascade)
}